# Dependencies
/node_modules/
/.rpt2_cache/

# Build / type caches
*.tsbuildinfo
.eslintcache

# Coverage / test output
coverage/

# Editors / IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*

# Logs
*.log
npm-debug.log*

# Local working files (Claude prompts, notes)
prompt-*.md

# Local agent dir
.claude/

# NOTE: dist/ is intentionally committed so the card can be installed via raw
# URL without a release. Do not ignore it.

# Single-file release build (dist-release/dreame-vacuum-card.js) — built on demand
# by release.yml via SINGLE_FILE=1, never committed.
/dist-release/
